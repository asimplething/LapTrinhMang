# Giao diện bắt gói tin
capture_interface="Wi-Fi"

# Thời gian bắt gói tin để lưu thành pcapng
capture_duration=5

# Số lượng gói tin bắt được trong capture_duration, tránh tình trạng bắt quá nhiều gói tin
maximum_packets_capture=200

# Đường dẫn file bắt được
output_capture_file="content/captured_network.pcapng"

# Quy định giới hạn băng thông mạng của hệ thống
minimum_network_limit="1 Kbs"
maximum_network_limit="1 Kbs"
